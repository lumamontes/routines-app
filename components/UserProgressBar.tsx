import React from "react";
import { useAtom } from "jotai";
// import { totalItemsTodayAtom, completedItemsTodayAtom } from "@/atoms";
import { Text, View } from "react-native";

export default function UserProgressBar() {
  // const [totalItemsToday] = useAtom(totalItemsTodayAtom);
  // const [completedItemsToday] = useAtom(completedItemsTodayAtom);
  <Text>Progress Bar</Text>;
}